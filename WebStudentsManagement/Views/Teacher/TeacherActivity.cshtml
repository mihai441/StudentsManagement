@model AllStudentsOnActivity
@{
    var studentsList = Model.Students.ToList();
    ViewData["Title"] = studentsList[0].
}
<h3>@Model.ActivityName</h3>
<div class="row">
    <div class="col-md-6">
        <h4>All students at this activity</h4>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Student Name</th>
                </tr>
            </thead>
            <tbody>
                @{
                    if (studentsList.Count != 0)
                    {
                        foreach (var student in studentsList)
                        {
                            <tr>
                                <th scope="row">
                                    @student.Name
                                    @student.SubscribedActivities
                                </th>
                                <td>
                                    <!--
                                    <a asp-action="TeacherActivityAdd" asp-route-activityId="@student.ActivityId" asp-route-studentId="@Model.Id[iterator]">Add</a> |
                                    <a asp-action="TeacherActivityDetails" asp-route-activityId="@student.ActivityId" asp-route-studentId="@Model.Id[iterator]">Details</a>
                                    -->
                                </td>
                            </tr>
                        }
                    }
                }
            </tbody>
        </table>
        <a asp-action="Index">Back to activity list</a>
    </div>
</div>
@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}